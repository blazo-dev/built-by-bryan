---
const { label, iconClass } = Astro.props;
---

<li class="skill-icon text-center list-none">
    <i class={`text-4xl mb-2 ${iconClass}`} aria-hidden="true"></i>
    <p class="text-sm font-medium">{label}</p>
</li>
